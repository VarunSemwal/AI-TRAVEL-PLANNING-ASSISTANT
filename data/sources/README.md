# Source documents

Place downloaded or manually prepared Singapore travel documents in this directory.
Each document must have a corresponding source record containing its title, URL,
retrieval date, and reuse/licensing notes. Run `scripts/build_index.py` after adding
or updating documents to regenerate the FAISS index.